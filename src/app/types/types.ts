export interface TableRow {
    id: number,
    name: string,
    email: string,
    age: number,
    city: string,
    company: string
}

export interface Column {
    id: string,
    label: string
};